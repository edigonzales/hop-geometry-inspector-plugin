# Two-input replay

Record complete caches for Source, Other and Result in one inspection.
Select the Source and Other caches and Run FROM Result. Four joined rows must
match the initial Result cache; both readers are replaced with cache sources.
The embedded source rows are already sorted by id.
