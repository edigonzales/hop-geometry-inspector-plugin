# Configured error stream

Open `inspect-errors.hpl`. Inspect Convert and select its Main and Errors output.
The two valid integer rows and one conversion error have different schemas.
Select Errors to see the original value and error description.
