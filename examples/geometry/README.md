# Geometry

Open [inspect-geometry.hpl](inspect-geometry.hpl) in Hop Desktop. Data is embedded in the Data Grid; no paths, network sources or extra plugins are required.

Inspect Result. Expect four rows, including malformed, null and empty geometry. Select each row and read its diagnostic. The valid LV95 point supports the swisstopo background.
