# Streams

Open [inspect-streams.hpl](inspect-streams.hpl) in Hop Desktop. Data is embedded in the Data Grid; no paths, network sources or extra plugins are required.

Inspect Filter and choose its TARGET streams Passed and Rejected. Expect one passed row and three rejected rows. Main is empty because this transform uses directed routing.
