# Rows

Open [inspect-rows.hpl](inspect-rows.hpl) in Hop Desktop. Data is embedded in the Data Grid; no paths, network sources or extra plugins are required.

Inspect Result. Expect three rows without a map. Sort id numerically: 1, 2, 10. Filter id > 1 to retain two rows.
